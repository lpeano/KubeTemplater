{{- if and .Values.webhook.enabled (eq .Values.webhook.certificateMode "self-signed") }}
# Secret is automatically created and managed by the operator's cert-manager component.
# Do NOT create an empty secret here as it would interfere with automatic certificate generation.
# The operator will create this secret with proper certificates on startup if it doesn't exist,
# or regenerate certificates if they are expired or invalid.
{{- end }}
